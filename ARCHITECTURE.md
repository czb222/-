# QuantStock Pro - A股量化选股系统

## 项目架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         前端层 (Streamlit)                              │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐            │
│  │  大盘概览   │  智能选股   │  个股详情   │  策略回测   │            │
│  └──────┬──────┴──────┬──────┴──────┬──────┴──────┬──────┘            │
└─────────┼─────────────┼─────────────┼─────────────┼───────────────────┘
          │             │             │             │
          ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         UI组件层 (ui/components.py)                    │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐            │
│  │  指标卡片   │   K线图表   │  风控面板   │  新闻展示   │            │
│  └─────────────┴─────────────┴─────────────┴─────────────┘            │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       策略层 (strategies/stock_picker.py)              │
│  ┌─────────────┬─────────────┬─────────────────────────────────────┐   │
│  │  选股过滤   │  回测引擎   │          风控计算                    │   │
│  └─────────────┴─────────────┴─────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       数据层 (data/market_data.py)                     │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐            │
│  │  大盘数据   │  股票列表   │  历史行情   │   新闻资讯   │            │
│  └─────────────┴─────────────┴─────────────┴─────────────┘            │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           数据源 (akshare)                            │
└─────────────────────────────────────────────────────────────────────────┘
```

## 项目目录结构

```
quantstock_pro/
├── app.py                    # 主入口文件
├── settings.py               # 配置管理（主题、策略参数、缓存配置）
├── ARCHITECTURE.md           # 架构文档
├── data/                     # 数据层
│   └── market_data.py        # 市场数据获取与缓存
├── strategies/               # 策略层
│   └── stock_picker.py       # 选股策略、回测、风控计算
└── ui/                       # UI层
    ├── components.py         # 可复用UI组件
    └── pages.py              # 页面视图
```

## 模块职责说明

### 1. 配置层 (settings.py)
- **主题配置**: 定义全局颜色、字体、样式变量
- **策略配置**: 选股参数（涨幅范围、量比阈值、止盈止损比例）
- **缓存配置**: 各数据源的缓存时间(TTL)
- **情绪词典**: 利多/利空关键词列表
- **主题应用**: 统一应用CSS样式

### 2. 数据层 (data/market_data.py)
- **get_market_overview**: 获取三大指数实时数据
- **get_stock_list**: 获取A股全市场股票列表
- **get_stock_history**: 获取个股历史K线数据（含均线）
- **get_stock_news**: 获取个股最新新闻资讯
- **get_stock_realtime_price**: 获取个股实时价格
- **get_stock_name**: 根据代码获取股票名称

### 3. 策略层 (strategies/stock_picker.py)
- **filter_stocks**: 执行选股策略，返回符合条件的股票
- **run_backtest**: 策略回测引擎，模拟历史交易
- **calculate_risk_management**: 计算止损价、止盈价、浮动盈亏

### 4. UI组件层 (ui/components.py)
- **render_metric_card**: 指标卡片组件
- **render_market_cards**: 大盘指标卡片组
- **render_kline_chart**: K线图组件
- **render_news_section**: 新闻列表组件（含情绪标记）
- **render_stop_loss_alert**: 止损警告组件
- **render_risk_management_panel**: 风控面板组件
- **render_backtest_summary**: 回测结果摘要
- **render_cumulative_return_chart**: 收益曲线图
- **render_download_button**: CSV下载按钮

### 5. 页面视图层 (ui/pages.py)
- **page_market_overview**: 大盘概览页
- **page_stock_picker**: 智能选股页
- **page_stock_detail**: 个股详情页（含风控+新闻）
- **page_backtest**: 策略回测页

## 数据流说明

### 选股流程
1. 用户在「智能选股」页设置参数并点击选股
2. `page_stock_picker` 调用 `filter_stocks`
3. `filter_stocks` 调用 `get_stock_list` 获取全市场股票
4. 应用筛选条件（涨幅、量比、排除ST/科创板）
5. 返回筛选结果并渲染表格

### 个股详情流程
1. 用户输入股票代码并查询
2. `page_stock_detail` 调用 `get_stock_history` 获取历史数据
3. 渲染K线图和基本信息
4. 用户输入买入价和持仓金额
5. 调用 `calculate_risk_management` 计算风控参数
6. 调用 `get_stock_news` 获取新闻并进行情绪分析

### 回测流程
1. 用户选择时间范围并启动回测
2. `page_backtest` 调用 `run_backtest`
3. `run_backtest` 遍历股票，应用选股条件
4. 模拟每日买入持有1天卖出
5. 计算收益率、最大回撤、胜率、盈亏比
6. 渲染统计结果和收益曲线

## 缓存策略

| 数据类型 | TTL (秒) | 说明 |
|---------|----------|------|
| 大盘数据 | 60 | 实时指数数据 |
| 股票列表 | 120 | A股股票列表 |
| 股票历史 | 300 | 个股K线数据 |
| 新闻资讯 | 60 | 个股新闻 |

## 核心策略参数

```python
STRATEGY_CONFIG = {
    'min_change': 3.0,      # 最小涨幅(%)
    'max_change': 9.0,      # 最大涨幅(%)
    'min_volume_ratio': 1.5, # 最小量比
    'stop_loss_pct': 5.0,    # 止损比例(%)
    'take_profit_pct': 5.0,  # 止盈比例(%)
}
```

## 技术栈

- **框架**: Streamlit 1.57+
- **数据**: akshare (A股数据接口)
- **图表**: Plotly (交互式图表)
- **数据处理**: Pandas
- **样式**: CSS (自定义深色主题)

## 启动方式

```bash
# 安装依赖
pip install akshare pandas streamlit plotly

# 运行应用
streamlit run app.py
```